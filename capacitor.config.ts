import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fitmantra.fitmantra',
  appName: 'fitmantra',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
